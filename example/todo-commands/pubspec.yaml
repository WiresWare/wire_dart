name: wire_example_todo_commands
description: A web app that uses only native HTML components
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  wire: ^1.5.3
  wire_example_shared:
    path: ../_shared
  wire_example_todo:
    path: ../todo

  shared_preferences:

dependency_overrides:
  wire:
    path: ../..

dev_dependencies:
  build_web_compilers: ^3.2.7
  build_runner: ^2.3.2
  build_test:
