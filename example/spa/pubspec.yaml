name: wire_spa
description: An absolute bare-bones web app.
# version: 1.0.0
#homepage: https://www.example.com

environment:
  sdk: "^3.10.4"

dependencies:
  wire: ^1.6.0
  states: ^1.1.0
  sporran: ^6.2.0
  tuple: ^2.0.1
  web: ^1.1.1
  http: ^0.13.6
  lint: ^2.8.0

dev_dependencies:
  build_runner:
  build_web_compilers:

dependency_overrides:
  wire:
    path: ../..
